<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thuexe" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" >
        <activity
            android:name=".new_signUp"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".carsActivity"></activity>
        <activity android:name=".signInActivity"></activity>
        <activity android:name=".signUpActivity"></activity>
        <activity android:name=".personalSettingActivity"></activity>
        <activity android:name=".carDetailActivity"></activity>
        <activity android:name=".bookingCarActivity"></activity>
        <activity android:name=".personalInfomationActivity"></activity>
        <activity android:name=".bookmarkActivity"></activity>
    </application>

</manifest>